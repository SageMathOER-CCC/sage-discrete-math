<?xml version="1.0" encoding="UTF-8"?>

<section xml:id="isomorphism" xmlns:xi="http://www.w3.org/2001/XInclude">
  <title>Isomorphism</title>
  <p>
    <me>
      G = (V, E) \sim G' = (V', E') \\
    </me>
    <me>
      G \text{ is isomorphic to } G' \text{ if there exists a bijection } f: V \rightarrow V' \text{ such that } \{u, v\} \in E \Leftrightarrow \{f(u), f(v)\} \in E'
    </me>
  </p>
  <p>
    Easier to prove, not isomorphic. If one of these fails then the graphs are not isomorphic. If all of these are true then the graph may or may not be isomorphic. Invariant under isomorphism:
    <me>
      \text{If } G = (V, E) \sim G' = (V', E'), \text{ then}
    </me>
    <me>
      G = (V, E) \text{ is connected iff } G' = (V', E') \text{ is connected}
    </me>
    <me>
      |V| = |V'| \text{ and } |E| = |E'|
    </me>
    <me>
      \text{degree sequence of } G = \text{ degree sequence of } G'
    </me>
  </p>
  <sage>
    <input>
      # example sage code
    </input>
  </sage>
</section>