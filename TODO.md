## TODOs
* simplify the updatd build script:
  * remove inkspace built-in conversion (instead convert svg file, and provide both pdf and svg versions)
  * use the script to update references as before and perform the build as usual (no in-time type conversion)

* Book Content:
* Handling IOs operations (inputs/outputs):
  * import data from local txt or csv file (graph definition), or remote/online resource (Google Sheet)
  * export (write/save) data to local file
  * export images (graphs) to local storage
